{
  "name": "traveltrack-webapp",
  "version": "1.0.0",
  "private": true,
  "description": "TravelTrack monorepo — React Native frontend + Node.js backend",
  "scripts": {
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm run dev"
  }
}
