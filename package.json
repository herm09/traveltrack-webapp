{
  "name": "traveltrack-webapp",
  "version": "1.0.0",
  "private": true,
  "description": "TravelTrack monorepo — Flutter frontend + Node.js backend",
  "scripts": {
    "frontend": "cd frontend && flutter run",
    "backend": "cd backend && npm run dev"
  }
}
